# Normalize line endings to LF in the repo so shell scripts and configs run
# correctly on the Linux VPS (avoids the CRLF "bad interpreter" failure).
* text=auto eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
